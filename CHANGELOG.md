# CHANGELOG

このファイルには、このプロジェクトの主な変更を記録します。
ユーザー影響のある変更を優先して記載します。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) を採用し、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。
運用ルールとテンプレート（比較リンクを含む）は `docs/release/ReleaseNotes.md` を参照してください。

## [Unreleased]

### Added

- `Ctrl+Shift+V` でクリップボード内容をファイル保存できる常駐アプリを追加。ホットキーは設定で変更可能。
- タスクトレイ常駐に対応。左クリックで設定画面、右クリックで「設定 / スタートアップ設定を開く / 通知設定を開く / バージョン情報 / 終了」を開ける。
- 同一ユーザー・同一セッションでの多重起動防止を追加。再起動時は既存インスタンスの設定画面を表示する。
- 保存先の自動決定に対応。デスクトップ、またはアクティブなエクスプローラーフォルダへ保存できる。
- クリップボード内容の自動判別に対応。画像、CSV、JSON、Markdown、テキストを優先順で保存する。
- 画像の保存形式として `.png` / `.jpg` を追加。透過画像を `.jpg` 保存する場合は白背景で合成する。
- テキストの保存形式として `.txt` / `.md` / `.json` / `.csv` を追加。JSON は整形し、CSV は Excel 互換の BOM 付き UTF-8 で保存する。
- ファイル名ルールを追加。プレフィックス、タイムスタンプ `yyyyMMdd_HHmmss`、重複時サフィックス `_1`, `_2` 以降に対応する。
- 通知設定を追加。保存成功、コンテンツなし、エラーを個別に ON/OFF でき、通知全体は Windows 設定で管理する。
- 初回起動時にスタートアップ設定の案内通知を表示する。インストール直後の自動起動既定値は ON。
- UI 言語の切り替えに対応。日本語 / 英語を再起動なしで反映できる。
- About 画面とクラッシュダンプ出力を追加。バージョン情報を確認でき、未処理例外時の診断情報取得に対応する。

### Changed

- 画像とテキストが同時に存在する場合は、画像のみを保存する。
- 保存対象外の状況（対象外ウィンドウ、仮想フォルダ、保存処理中の再トリガー、無効化された種別）は通知せず静かに無視する。
