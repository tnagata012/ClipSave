# プライバシーポリシー

施行日: 2026-03-21

本プライバシーポリシーは、Microsoft Store、GitHub Releases、またはソースコードから配布される ClipSave の利用時に、ClipSave がどのように情報を取り扱うかを説明するものです。

ClipSave は、クリップボードの内容をローカル環境だけで処理することを前提に設計されています。クラウド同期、アクセス解析、テレメトリ、広告 SDK、およびユーザーのデータを開発者または第三者へ送信する組み込み機能はありません。

## ClipSave がアクセスし得る情報

ClipSave は、次の情報にアクセスする場合があります。

- ユーザーがコピーしたクリップボードの内容
  テキストおよび画像を含みます。
- ファイルの保存に必要なローカルの保存先情報
  デスクトップ、または現在 File Explorer で開いているフォルダーなどを含みます。
- ユーザーが設定したアプリ設定
  保存対象、画像形式、ホットキー、通知設定、表示言語などを含みます。
- 任意のローカル診断情報
  ログを有効にした場合のログ、または予期しないエラー発生時のクラッシュダンプを含みます。

ユーザーがコピーした内容には、個人情報が含まれる場合があります。ClipSave は、その内容をユーザーが要求した保存処理を実行するために、ユーザーのデバイス上でのみ処理します。

## 情報の利用目的

ClipSave は、取得した情報を次の目的でのみ使用します。

- クリップボードに画像またはテキストがあるかを判定するため
- コピーされたテキストをプレーンテキスト、Markdown、JSON、表形式データとして判別するため
- デバイス上のローカルフォルダーへファイルとして保存するため
- 画像形式、ホットキー、通知、表示言語などのユーザー設定を適用するため
- ユーザーがログを有効にした場合、またはアプリが未処理例外によりクラッシュダンプを書き出した場合に、障害解析を支援するため

ClipSave は、クリップボードの内容、保存したファイル、設定内容を、広告、分析、行動追跡、プロファイリングの目的では使用しません。

## データの保存と処理

ClipSave は、クリップボードの内容をユーザーのデバイス上でローカル処理します。

ClipSave は、次の情報をローカルに保存する場合があります。

- ユーザーが明示的に保存したファイル
- アプリ設定を保持する `settings.json`
- 高度なログを有効にした場合のログファイル
- 予期しないエラーが発生した場合のクラッシュダンプ

Microsoft Store からインストールされたパッケージ版では、アプリデータはアプリの `LocalState` 配下に保存されます。パッケージ化されていない実行形態では、Windows のアプリケーションデータ配下に保存されます。

ClipSave は、これらのデータを開発者へ自動送信しません。

## ログとクラッシュダンプ

ログ機能は、ユーザーが有効にしない限り既定で無効です。

ローカルのログおよびクラッシュダンプには、次のような技術情報が含まれる場合があります。

- 保存処理に関係するファイルパスやファイル名
- アプリ設定のスナップショット
- 例外メッセージおよびスタックトレース
- OS やランタイムの情報

ClipSave は、ログやクラッシュダンプを自動送信しません。サポートや不具合報告のためにそれらを手動で共有する場合、その共有はユーザー自身の判断によるものです。

## 第三者提供と共有

ClipSave は、クリップボードの内容、保存ファイル、設定、ログ、クラッシュダンプを、アプリを通じて開発者または第三者へ販売、貸与、共有、移転しません。

アプリには、ユーザーデータを外部へ送信する組み込みネットワーク通信機能はありません。

## セキュリティ

ClipSave は、データ露出を最小化するよう設計されています。

- クリップボード処理はローカルのみで行われます。
- テレメトリやアクセス解析は含まれていません。
- アカウント作成やサインインは不要です。
- 保存結果はユーザーのデバイス上のローカルフォルダーへ書き込まれます。
- 設定はローカルに保存されます。

ただし、いかなるソフトウェアも絶対的な安全性を保証するものではありません。ユーザー自身のデバイスへのアクセス管理、および共有前のファイル、ログ、ダンプの内容確認は、ユーザーの責任で行ってください。

## ユーザーの選択と制御

ユーザーは、ClipSave によるデータ処理を次の方法で制御できます。

- 保存したい内容をコピーしたときだけアプリを使用する
- 保存実行前に、クリップボードへ置く内容を自分で選ぶ
- アプリ設定で保存対象、画像形式、通知、ホットキー、表示言語などを変更する
- ローカル診断ログが不要な場合は高度なログを無効のままにする
- ClipSave が作成したファイルを任意のタイミングで削除する
- ローカルの設定、ログ、クラッシュダンプを任意のタイミングで削除する

ClipSave には、ユーザーアカウントやクラウド保存の仕組みがありません。そのため、開発者がユーザーのローカルなクリップボード内容や保存ファイルへ独自にアクセスすることはありません。

## 子どものプライバシー

ClipSave は一般向けのユーティリティアプリであり、子ども向けに設計されたものではありません。また、バックエンドサービスを提供していないため、子どもの個人情報をオンラインで収集する仕組みはありません。

## 本ポリシーの変更

ClipSave の機能変更に応じて、本プライバシーポリシーを更新する場合があります。最新版はこのリポジトリで公開します。

## お問い合わせ

本プライバシーポリシーに関するお問い合わせ先:

- Email: `tnagata012@gmail.com`
- Repository: https://github.com/tnagata012/ClipSave
