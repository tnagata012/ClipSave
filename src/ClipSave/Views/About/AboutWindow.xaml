<Window x:Class="ClipSave.Views.About.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Localizer[AboutWindow_Title], Mode=OneWay}"
        Width="400"
        Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="pack://application:,,,/ClipSave;component/Assets/ClipSave.ico">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,16">
            <Image Source="/Assets/ClipSaveLogo.png"
                   Width="64" Height="64"
                   HorizontalAlignment="Center"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Margin="0,0,0,12"/>
            <TextBlock Text="{Binding ApplicationName}"
                       FontSize="24"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>
            <TextBlock HorizontalAlignment="Center" Margin="0,4,0,0">
                <Run Text="{Binding Localizer[AboutWindow_VersionPrefix], Mode=OneWay}"/>
                <Run Text="{Binding Version, Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>

        <Separator Grid.Row="1" Margin="0,8"/>

        <StackPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBlock Text="{Binding Localizer[AboutWindow_SystemInfoTitle], Mode=OneWay}"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Localizer[AboutWindow_DotNetRuntimeLabel], Mode=OneWay}"
                           Foreground="Gray" Margin="0,2"/>
                <TextBlock Grid.Row="0" Grid.Column="1"
                           Text="{Binding DotNetVersion, Mode=OneWay}"
                           TextTrimming="CharacterEllipsis"
                           ToolTip="{Binding DotNetVersion, Mode=OneWay}"
                           Margin="0,2"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding Localizer[AboutWindow_OsLabel], Mode=OneWay}"
                           Foreground="Gray" Margin="0,2"/>
                <TextBlock Grid.Row="1" Grid.Column="1"
                           Text="{Binding OsVersion, Mode=OneWay}"
                           TextTrimming="CharacterEllipsis"
                           ToolTip="{Binding OsVersion, Mode=OneWay}"
                           Margin="0,2"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding Localizer[AboutWindow_BuildDateLabel], Mode=OneWay}"
                           Foreground="Gray" Margin="0,2"/>
                <TextBlock Grid.Row="2" Grid.Column="1"
                           Text="{Binding BuildDate, Mode=OneWay}"
                           Margin="0,2"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" VerticalAlignment="Bottom" Margin="0,16,0,0">
            <Separator Margin="0,0,0,8"/>
            <TextBlock Text="{Binding Copyright, Mode=OneWay}"
                       HorizontalAlignment="Center"
                       Foreground="Gray"
                       FontSize="11"/>
        </StackPanel>

        <Button Grid.Row="4"
                Content="{Binding Localizer[Common_Close], Mode=OneWay}"
                Command="{Binding CloseCommand}"
                HorizontalAlignment="Right"
                Margin="0,16,0,0"
                Padding="16,6"
                IsDefault="True"
                IsCancel="True"/>
    </Grid>
</Window>

